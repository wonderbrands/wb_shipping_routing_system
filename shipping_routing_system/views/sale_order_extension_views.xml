<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_sale_order_form_extension" model="ir.ui.view">
        <field name="name">sale.order.form.extension</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">
            <!-- Insertamos nuestra nueva pestaña en el notebook existente -->
            <xpath expr="//notebook" position="inside">
                <!-- Nueva pestaña para el Shipping Routing System -->
                <page string="Shipping-Routing info">
                    <group>
                    <field name="wb_srs_flag"/>
                    </group>
                    <group>
                        <field name="product_name" readonly="1"/>
                        <field name="product_dimensions" readonly="1"/>
                    </group>
                    <group string="Opciones de paqueterías">
                        <field name="wb_shipping_options">
                            <tree editable="bottom">
                                <field name="index"/>
                                <field name="carrier"/>
                                <field name="price"/>
                            </tree>
                        </field>
                    </group>
                </page>
            </xpath>
        </field>
    </record>
</odoo>
